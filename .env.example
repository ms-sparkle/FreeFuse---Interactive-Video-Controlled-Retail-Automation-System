# -----------------------------------------------------------------------------
# FreeFuse Environment Variables Template
#
# Instructions:
# 1. Copy this file to a new file named '.env' in the project root.
# 2. Replace the placeholder values with our actual configuration details.
# -----------------------------------------------------------------------------

# -- General Application Configuration --
LOG_LEVEL=info

# -- Backend API Configuration --
API_PORT=8000
API_BASIC_AUTH_USERNAME=admin
API_BASIC_AUTH_PASSWORD=changeme

# -- MQTT Broker Configuration --
# These variables are used by all services that connect to the MQTT broker.
MQTT_BROKER_HOST=mqtt
MQTT_BROKER_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# -- Frontend Kiosk Configuration --
KIOSK_PORT=3000
# The URL of the backend API that the kiosk will connect to.
# This should be the public-facing URL when deployed.
REACT_APP_API_BASE_URL=http://localhost:8000

# -- CV Service Configuration --
# This could be a path to a video file, a camera index (e.g., 0), or a network stream URL.
CV_VIDEO_SOURCE=0
